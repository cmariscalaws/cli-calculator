[project]
name = "cli-calculator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.116.1",
    "pydantic>=2.11.7",
    "pytest>=8.4.1",
]

[dependency-groups]
dev = [
    "pydantic>=2.11.7",
]
